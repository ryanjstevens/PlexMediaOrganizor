# Plex Media Organizor
Console application to update folder structure and files names based on Plex Media server database.

Plex server will need to re-scan affected collections after running the organizer.
